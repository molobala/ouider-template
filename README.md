# OUUDesigner render example project
This project is a webpack based project, depend on [`ouider`](https://www.npmjs.com/package/ouider) package
## Build
To build this project, run in your terminal
`npm run build`
## Run
To previsualise in dev server, run below command:
`npm run dev`